/* Compute the Fibonacci of an integer */

fibo: proc (n int, g int loc);
  dcl h int;
  if n < 3 then
    g = 1;
    return;
  else
    fibo(n-1, h);
    fibo(n-2, g);
    g = h + g;
  fi;
  //print(-1000,n,g);
end;

dcl k int = 0;
dcl i int;
do while i < 10;
fibo(i,k);
print(k);
i += 1;
od;
